---
export interface Props {
  href: string;
  label: string;
}

const { href, label } = Astro.props;
---

<a
  href={href}
  class="nav-link relative transition-colors duration-200 hover:text-zinc-100"
  role="listitem"
  data-nav-link
>{label}</a>
